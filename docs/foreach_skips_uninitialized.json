{
    "image": "foreach_skips_uninitialized.png",
    "date": "2024-05-26",
    "title": "Foreach() Skips Uninitialized Properties",
    "content": "Foreach() reads naturally all public properties in an object. Protected and private are omitted, unless in the right context.\nForeach() also skips silently uninitialized properties: this prevents the generation of NULL values, but also, skips all readonly properties: any direct hit on such property would otherwise generate a Fatal error.",
    "seeAlso": {
        "Foreach (PHP manual)": "https://www.php.net/manual/en/control-structures.foreach.php#control-structures.foreach"
    },
    "tags": [
        "silent"
    ],
    "author": "",
    "contact": "",
    "3v4l": {
        "foreach skips the empty values": "https://3v4l.org/cAniT"
    },
    "code": "<?php\n\nclass z {\n    public readonly int $p;\n    public readonly int $q;\n\n    public function __construct()\n    {\n        $this->p = 1;\n    }\n}\n\n$x = new z;\n\n\/\/ foreach skips undefined properties, readonly or not\nforeach($x as $k => $v) {\n    print $k . \" \" . $v . PHP_EOL;\n}\n\necho $x->q;\n?>",

    "phpError": {},
    "features": [
        "foreach",
        "readonly",
        "object"
]
}